package com.oopman.collectioneer.cli.actions.imprt

import com.oopman.collectioneer.cli.Config
import io.circe.*
import io.circe.generic.auto.*
import io.circe.syntax.*

object Database:
  def importDatabase(config: Config) =
    "".asJson


