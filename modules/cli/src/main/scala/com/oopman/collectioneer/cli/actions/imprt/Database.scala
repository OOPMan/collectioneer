package com.oopman.collectioneer.cli.actions.imprt

import com.oopman.collectioneer.cli.CLIConfig
import io.circe.*
import io.circe.generic.auto.*
import io.circe.syntax.*

object Database:
  def importDatabase(config: CLIConfig) =
    "".asJson


